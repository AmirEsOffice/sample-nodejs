function PostCardData(name, description, category) {  
    this.name = name || null;
    this.description = description || null;
    this.category = category || null;
    this.title = title || null;
    this.imageData = imageData || null;
}

module.exports = PostCardData;